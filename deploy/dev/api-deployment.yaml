apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-app-api
spec:
  selector:
    matchLabels:
      app: demo-app
  replicas: 1
  template:
    metadata:
      labels:
        app: demo-app
    spec:
      containers:
      - name: demo-app
        image: yjring/demo_app:dev-fb476a
        ports:
        - containerPort: 8080
